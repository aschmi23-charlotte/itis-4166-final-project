{
  "name": "itis-4166-final-project",
  "version": "1.0.0",
  "description": "A Node.js + Express + Prisma To-Do List API",
  "main": "src/server.js",
  "scripts": {
    "start:dev": "node --env-file=.env --watch src/server.js",
    "start:prod": "node src/server.js",
    "migrate:dev": "npx prisma migrate dev",
    "migrate:deploy": "npx prisma migrate deploy",
    "generate:client": "npx prisma generate client",
    "generate:test_plan": "node docs/test_plan.js",
    "generate:test_plan:env": "node --env-file=.env docs/test_plan.js",
    "build:dev": "npm install && npm run migrate:deploy && npm run generate:client && npm run seed:dev",
    "build:prod": "npm install --omit=dev && npm run migrate:deploy && npm run generate:client && npm run seed:prod",
    "seed:dev": "node --env-file=.env prisma/seed.js",
    "seed:prod": "node prisma/seed.js"
  },
  "keywords": [
    "node",
    "express",
    "prisma",
    "postgres",
    "api",
    "tasks"
  ],
  "author": "aschmi23-charlotte",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/adapter-pg": "^7.4.2",
    "@prisma/client": "^7.4.2",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.3.2",
    "express-validator": "^7.2.1",
    "js-yaml": "^4.1.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "prisma": "^7.3.0",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.36.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^16.4.0",
    "json-yaml-ref-resolver": "^0.0.11",
    "lodash": "^4.18.1",
    "prettier": "^3.6.2"
  }
}
